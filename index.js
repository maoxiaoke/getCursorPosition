const getCursorPosition = require('./cursor-position');

getCursorPosition((error, position) => {
  // console.log('error', error);
  // console.log('row:', position, 'column:', position, position);
});
